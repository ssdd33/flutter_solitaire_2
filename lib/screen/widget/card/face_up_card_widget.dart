import 'package:flutter/material.dart';

class FaceUpCardWidget extends StatelessWidget {
  const FaceUpCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
